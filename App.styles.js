import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
      flex: 1,
    },

    text_light: {
        color: '#000'
    },

    text_dark: {
        color: "#fff"
    }
 });

export default styles;